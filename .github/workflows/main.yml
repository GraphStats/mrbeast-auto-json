name: Fetch MrBeast Subs

on:
  schedule:
    - cron: "*/5 * * * *" # toutes les 5 minutes
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch_subs:
    runs-on: ubuntu-latest
    steps:
      - name: Decide if run
        id: decide
        run: |
          # 70% de chance d'exécuter
          if [ $(( RANDOM % 10 )) -lt 7 ]; then
            echo "run=true" >> $GITHUB_OUTPUT
            echo "✅ Run (70% chance)"
          else
            echo "run=false" >> $GITHUB_OUTPUT
            echo "❌ Skip (30% chance)"
          fi

      - name: Checkout repo
        if: steps.decide.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch subscriber count
        if: steps.decide.outputs.run == 'true'
        run: |
          curl -s https://backend.mixerno.space/api/youtube/estv3/UCX6OQ3DkcsbYNE6H8uQQuVA \
          | jq '{subscriberCount: .items[0].statistics.subscriberCount}' > mrbeast.json

      - name: Commit and push
        if: steps.decide.outputs.run == 'true'
        run: |
          git config user.name "GraphStats"
          git config user.email "no.replybot45455@gmail.com"
          git add mrbeast.json
          git commit -m "Update subscriber count" || echo "No changes to commit"
          git push
